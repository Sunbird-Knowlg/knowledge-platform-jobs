include "base-test.conf"

kafka {
  input.topic = "flink.issue.certificate.request"
  groupId = "flink-certificate-pre-processor-group"
  output.topic = "flink.generate.certificate.request"
  output.failed.topic = "flink.certificate.events.failed"
}

task {
  consumer.parallelism = 1
  parallelism = 1
}

lms-cassandra {
  keyspace = "sunbird_courses"
  batchTable = "course_batch"
  userTable = "user_enrolments"
  assessmentAggregatorTable = "assessment_aggregator"
  host = "localhost"
  port = "9142"
}

dp-redis {
  host = localhost
  port = 6340
  database.index = 5
}

content.search.basePath = "http://localhost/search"
lms.basePath = "http://localhost/lms"
content.basePath = "http://localhost/content"
cert.basePath = "http://localhost/certs"
learner.basePath = "http://localhost/learner"

content.v3.read = "/content/v3/read/"
user.v1.search = "/private/user/v1/search"
org.v1.read = "/v1/org/read"