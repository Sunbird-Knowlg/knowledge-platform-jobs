include "base-test.conf"

kafka {
  input.topic = "sunbirddev.learning.graph.events"
  groupId = "sunbirddev-mvc-indexer-group"
  output.failed.topic = "sunbirddev.mvc.events.failed"
}

task {
  consumer.parallelism = 1
  parallelism = 1
  window.time = 60
  producer.parallelism = 1
}

lms-cassandra.keyspace = "local_content_store"
lms-cassandra.table = "content_data"

timezone = "IST"

nested.fields="trackable,credentials"

learning_service.basePath = "http://localhost:8080/learning-service"

ml.vector.api="11.2.4.22"
ml.keyword.api="11.2.4.22"
